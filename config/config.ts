export const pgDbConfig = {
  host: "localhost",
  database: "erp",
  user: "postgres",
  password: "hari",
  port: 5432,
  max: 40, //max no of client in the pool
  idleTimeoutMillis: 30000,
};
